# Stage 1 Gap Inventory

Date: 2026-06-21
Source blueprint: `Docs/Stage1_20260621_blueprint.md`
Status: execution inventory, not launch evidence

This inventory maps the Stage 1 blueprint into trackable work areas. It does not close any release gate by itself. CI, Staging, Production, and Do-Not-Launch status must still be computed from exact validator-readable evidence.

## Current Production Launch Snapshot

Status: current blocker inventory, not launch evidence.
Snapshot source: `ops/evidence/non_clearing/production-non-clearing-refresh.json`.
Refresh command: `python3 scripts/refresh_stage1_production_non_clearing_evidence.py`.
Validator command: `python3 scripts/validate_stage1_production_non_clearing_refresh.py`.

- Stage1 gates: `6/14 = 42.9%`; release decision `no_go`.
- Staging runtime: `pass / go`.
- External resources: `5/7 = 71.4%`.
- Production required inputs: `2/60 = 3.3%`; missing `55`, invalid `3`, blocking `58`.
- Production source probes: `0/4` ready, `4` blocked, `58` blocking inputs.
- Non-clearing refresh steps: `35/38` passed, `3` blocked, `0` failed, `0` unexpected exits.
- `production_dns_https`: `50.0%`, blockers `2`, first blocker `system resolver failed for zenari.ai: [Errno 8] nodename nor servname provided, or not known`.
- `production_live_billing`: `0.0%`, blockers `20`, first blocker `billing_proof_missing`.
- `production_security_runtime`: `0.0%`, blockers `10`, first blocker `security_proof_missing`.
- `production_governance_release`: `0.0%`, blockers `26`, first blocker `governance_proof_missing`.

Non-current blockers:

- R2 bucket `zenari`: not current blocker.
- Stripe sandbox: not current blocker; production live evidence is still required.
- z.ai/OpenAI-compatible provider: not current blocker.
- Staging Azure and local dev ports: not current blockers for production launch.
- `worker` / `crawler` / `migrate`: backend runtime entrypoints only, not release images.
- `manager`: legacy local-only, not release surface.

Staging/preflight evidence does not clear production gates. Production launch remains `no_go` until strict production evidence passes.
Release images remain closed to `web`, `admin`, and `backend`; `worker`, `crawler`, and `migrate` stay backend runtime targets, and `manager` stays legacy local-only.

## Current Root State

- `R-1` and `R-2`: Stage 1 authority and Stage 0 gate mapping exist in the blueprint.
- `R-3` and `R-4`: Local Stripe sandbox values belong in ignored `.env`; `.env.example` keeps placeholders; `scripts/stripe_sandbox_selftest.sh` is the default Stripe check.
- `R-5` and `R-6`: Public brand text has been migrated to `zenari.ai` across the active user/admin/manager surfaces and API display names verified by text scan and typecheck. Lowercase package names, database names, OpenAPI file names, old evidence paths, and generated-client file names remain compatibility markers until a dedicated migration lands.
- `R-7`: This file is the first machine-trackable gap inventory.
- `R-8`: `scripts/stage1_scope_guard.py` validates checklist scope, local ports, and placeholder-secret policy.
- `VF-2`: `scripts/validate_stage1_batch_generation_contract.py` now validates Stage 1 batch fixtures, quota accounting, trace references, retry/dead-letter policy anchors, progress counts, user-projection redaction boundaries, and the admin queue read API contract for batch runtime and child task projections. This is local API-backed contract evidence, not proof that deployed API/worker fan-out is production-ready.
- `VF-2load`: `scripts/validate_stage1_load_evidence.py` now validates the OP-7 Stage 1 load evidence contract across `scripts/load_smoke.sh`, canonical `ops/evidence/staging/stage1-load.json`/`.ndjson` paths, seven required load modes, p95/error/queue/provider/export/billing webhook thresholds, safe projection policy, and `fixtures/stage1/load/local_contract.json`. `LOAD_MODE=preflight_stage1 scripts/load_smoke.sh` writes `ops/evidence/staging/stage1-load.preflight.json` to verify non-local HTTPS staging targets, full `RELEASE_SHA`, request/concurrency thresholds, and explicit canonical write opt-in before the operator runs `WRITE_CANONICAL_STAGE1_LOAD_EVIDENCE=1 LOAD_MODE=all scripts/load_smoke.sh`; this preflight cannot clear Stage 1 staging runtime. Contract-only and preflight validation are wired into `scripts/repo_validate.sh`; the aggregate Stage 1 staging runtime gate now consumes this validator as a distinct strict child component with `load_ready`. Strict canonical Stage 1 load evidence remains open until production-like staging targets generate the required rows and metrics.
- `VF-2d`: `scripts/validate_stage1_user_batch_progress_contract.py` now validates the user-side `FE-10`/`WK-7` batch progress polling contract across OpenAPI `getBatchGeneration`/`listBatchGenerationChildren`/`getBatchGenerationProgress`, generated client metadata, `web/lib/batch-client.ts`, `DevZenariClient.refreshBatchGenerationProgress`, workspace polling attributes, result asset/canvas projections, focused web tests, `web/tests/stage1-batch-generation.spec.ts` local browser workflow coverage, and a local fixture. This is local API-backed polling and browser contract evidence; deployed-backend staging Playwright create/progress/cancel/retry workflow, SSE or production polling cadence evidence, and real provider result visibility remain open.
- `VF-2c`: `scripts/validate_stage1_batch_result_storage_contract.py` now validates the `AS-1` to `AS-3` batch result storage contract across `PostgresBatchResultSink`, object-store manifest refs, thumbnail refs, asset/canvas lineage, no-raw-provider-payload persistence, worker object-store injection, focused Go tests, and a local fixture. This is contract evidence; real provider image bytes, rendered thumbnails, signed URL, retention cleanup, and staging runtime evidence remain open.
- `VF-2a`: `scripts/validate_stage1_batch_quota_reconciliation.py` now validates a retry/dead-letter/manual-retry quota ledger fixture and code anchors for reserve, commit, refund, provider usage debit/credit reconciliation, and idempotent replay semantics. `scripts/validate_stage1_batch_quota_runtime_replay.py` now also validates local runtime replay evidence generated by `backend/internal/worker TestBatchQuotaRuntimeReplayProducesEvidenceFixture`, covering `BatchRunner.RunOnce`, `BatchChildExecutor.ExecuteClaimedChild`, manual retry re-reserves previously refunded quota, retry-attempt scoped quota idempotency, and provider usage debit/credit reconciliation. Local runtime replay evidence now covers provider usage debit/credit adjustments, but it remains local contract/runtime proof only. `scripts/validate_stage1_staging_quota_replay_evidence.py`, `scripts/generate_stage1_staging_quota_replay_evidence.py`, and `fixtures/stage1/staging_quota_replay/local_contract.json` now define strict staging quota replay evidence from real deployed Postgres/provider usage logs; `python3 scripts/generate_stage1_staging_quota_replay_evidence.py --preflight` writes `ops/evidence/staging/stage1-quota-replay.preflight.json` to verify non-local HTTPS staging API, real deployed Postgres URL presence without persisting it, and hashed tenant/batch refs before the canonical generator runs. Missing staging DB/API/batch inputs produce blocked diagnostics and cannot clear the gate. The aggregate `VF-6` staging runtime gate now consumes this validator as the `staging_quota_replay` component and exposes `runtime_input_readiness.quota_replay_ready`; Strict staging quota replay remains open. Staging validation remains incomplete until a real staging quota reconciliation replay generates canonical `ops/evidence/staging/stage1-quota-replay.json` plus `.ndjson` proving retry, dead-letter, manual retry re-reserve, provider usage debit/credit, and idempotent replay against production-like staging.
- `VF-2b`: `scripts/validate_stage1_provider_cost_reconciliation.py` now validates the `PR-9`/`BL-11` provider-cost reconciliation contract across `ProviderCostReconciler`, `provider_usage_logs`, `provider_model_capabilities`, `QuotaRepository.ReconcileProviderUsage`, batch executor quota-idempotency metadata, spend-cap/outlier flags, local fixture math, and focused Go tests. This is local contract evidence; real provider invoice/billing-period spend exports and staging quota transaction replay remain open.
- `VF-2e`: `scripts/validate_stage1_rate_limit_spend_cap_contract.py` now validates the local `BE-13` request-time rate-limit/spend-cap enforcement contract across `backend/internal/ratelimit`, MemoryStore/RedisStore, user/tenant/provider/admin-action scopes, explainable denial codes, safe audit metadata, rate-limit env defaults, non-local Redis-store validation, server route wiring for batch create/provider sandbox/admin billing mutations, and `fixtures/stage1/rate_limit_spend_cap/local_contract.json`. Redis-backed staging runtime evidence remains open, and production security launch evidence must still prove rate limit, provider spend cap/kill switch, audit visibility, and provider key containment before launch.
- `VF-2f`: `scripts/validate_stage1_api_error_taxonomy_contract.py` now validates the local `BE-14` API error taxonomy contract across `backend/internal/api`, shared server `writeError`, OpenAPI `ErrorEnvelope`/`ErrorTaxonomy`, focused backend tests, blueprint categories (`retryable`, `blocked`, `quota_insufficient`, `provider_unavailable`, `review_required`), top-level `taxonomy`/`retryable`/`blocked` fields, preserved backward-compatible envelope fields, and `fixtures/stage1/api_error_taxonomy/local_contract.json`. Frontend staging error-state evidence remains open, and production security evidence must still prove taxonomy stability and redaction across deployed billing/provider/batch/safety/auth failures.
- `VF-2g`: `scripts/validate_stage1_support_ticket_linking_contract.py` now validates the local `BE-12` support ticket linking/redaction contract across `backend/internal/support`, `stage0.Repository.CreateSupportTicket`/`ListSupportTickets`, OpenAPI `SupportTicketCreate`/`SupportTicket`, support ticket migrations, user support context, admin support linkage table, focused Go tests, and `fixtures/stage1/support_ticket_linking/local_contract.json`. The local contract requires project/task/batch/trace/asset/export/quota/billing links and redacts body/metadata before persistence/projection; support workflow staging evidence remains open, and production security evidence must still prove support tickets, analytics, admin tables, and logs never expose raw secrets, prompts, provider payloads, or billing payloads.
- `VF-2h`: `scripts/validate_stage1_trace_projection_contract.py` now validates the local `BE-10`/`BE-11` trace projection contract across `backend/internal/trace`, `PromptContextPayload`, `TraceProjection`, `PostgresBatchResultSink` trace projection wiring, OpenAPI `PromptContext`/`AgentTrace`, Stage 0 trace completeness/visibility validator anchors, focused trace/task Go tests, and `fixtures/stage1/trace_projection/local_contract.json`. The local contract keeps prompt text as internal context, exposes user/export prompt hashes plus selected objects, assets, brand, model hints, and tool hints, and rejects raw prompt/provider/safety payload or secret projection. Staging trace runtime evidence remains open, and production security evidence must still prove deployed traces, exports, support views, and logs do not expose raw prompt/provider/safety payloads or secrets.
- `VF-2i`: `scripts/validate_stage1_canvas_assets_contract.py` now validates the local `BE-6`/`BE-8`/`BE-9` canvas object, visual asset, and tenant isolation contract across `backend/internal/canvas`, `backend/internal/assets`, OpenAPI `CanvasNode`/`Asset` schemas, tenant FK/index migrations, `PostgresBatchResultSink` object/asset/canvas persistence anchors, focused Go tests, and `fixtures/stage1/canvas_assets/local_contract.json`. The local contract covers Stage 1 object types, transforms, z-index/locked/hidden state, asset refs, lineage refs, storage refs, thumbnail refs, raw-payload rejection, secret-like metadata rejection, tenant-scoped projections, and tenant predicates. Real API persistence reads, rendered assets/thumbnails, signed URL/retention proof, and staging runtime evidence remain open.
- `VF-2ia`: `scripts/validate_stage1_canvas_interaction_contract.py` now validates the local `FE-3` to `FE-7` canvas interaction contract across `CanvasInteractionState`, `CanvasTool`, `defaultCanvasInteraction`, `DevZenariClient` local select/drag/zoom/fit/tool/layer/duplicate/delete methods, the `stage1.canvas-interaction-user-contract` workspace marker, `stage1.canvas-toolbar`, `stage1.canvas-layers-panel`, focused client tests, focused workspace smoke tests, CSS anchors, and `fixtures/stage1/canvas_interaction/local_contract.json`. This proves a local canvas interaction contract for selection, move, zoom, fit, toolbar, layers hide/lock, and keyboard duplicate/delete. It does not close AS-4 backend versioning, AS-5 version history preview/diff, AS-6/AS-7 edit tools/mask UI, real editor-library integration, or staging Playwright evidence.
- `VF-2iaa`: `web/tests/stage1-canvas.spec.ts` now provides focused local browser coverage for the `FE-3` to `FE-7` canvas route, including toolbar tool switching, zoom/fit, candidate-to-canvas selection, move, lock/unlock, hide/show, keyboard duplicate/delete, edit-tool apply, canvas version restore, layer row state, and CSRF guard metadata for edit/restore actions. This is local Playwright contract evidence only; real editor-library integration, deployed API persistence, staging Playwright evidence, and production security evidence remain open.
- `VF-2ib`: `scripts/validate_stage1_canvas_versioning_contract.py` now validates the local `AS-4` and `AS-5` canvas versioning contract across `backend/internal/canvas/version.go`, focused Go version tests, `CanvasVersion` snapshot/diff/restorePreview state, `DevZenariClient.restoreCanvasVersion`, the `stage1.canvas-version-history` UI marker, focused workspace smoke tests, CSS anchors, and `fixtures/stage1/canvas_versioning/local_contract.json`. The local canvas versioning contract covers workspace snapshots, object-level diff, object/workspace restore plans, tenant/workspace validation, idempotent version SQL anchors, version preview attributes, restore CSRF guard metadata, and preserving later nodes when restoring an older snapshot. Staging Playwright evidence, real collaborative conflict runtime, real editor-library integration, and deployed persistence/runtime evidence remain open.
- `VF-2ic`: `scripts/validate_stage1_edit_tools_mask_contract.py` now validates the local `AS-6` and `AS-7` edit tools/mask contract across `backend/internal/edittools`, focused edit-tool Go tests, `EditToolState`/`EditMaskState`/`EditToolRevision`, `DevZenariClient.setEditTool`/`updateEditMask`/`applyEditTool`, the `stage1.edit-tools-mask-local-contract` UI marker, `Apply Edit Tool` CSRF guard, derived canvas node and asset-library revision projection, focused web tests, CSS anchors, and `fixtures/stage1/edit_tools_mask/local_contract.json`. The local edit tools/mask contract covers crop/rotate/flip/remove-background/upscale/erase/expand tool types, brush/rect/lasso masks, source-dimension mask alignment, original-asset retention, derived asset revisions, provider `image.edit` request construction, no raw provider payload persistence/projection, and secret-like request/mask rejection. Real provider edit adapter behavior, staging Playwright evidence, production security evidence, and deployed persistence/runtime evidence remain open.
- `VF-2id`: `scripts/validate_stage1_mentions_contract.py` now validates the local `FE-9` mention parser/picker contract across `web/lib/mentions.ts`, focused Vitest coverage, Workspace Brief UI anchors, and `fixtures/stage1/mentions/local_contract.json`. It covers `@object`, `@asset`, `@brand`, `@skill`, and allowed `@model` mentions using bracket labels or compact IDs, Chinese labels, spaces, duplicate mention de-duplication, unresolved mention projection, and non-allowed model blocking. This is local UI/parser contract evidence only; staging prompt composer evidence remains open.
- `VF-2ie`: `scripts/validate_stage1_prompt_composer_contract.py` now validates the local `FE-8` prompt composer payload contract across `web/lib/prompt-context.ts`, `PromptComposerPayload`/`PromptComposerContext` types, `DevZenariClient.createBatchGeneration` payload storage, Workspace prompt composer controls, focused Vitest coverage, workspace smoke coverage, and `fixtures/stage1/prompt_composer/local_contract.json`. The local contract projects visible selected object chips, accepted references, active reusable asset-library refs, active/default Brand Kit, allowed model hints, requested count, aspect ratio, quality, and `image.generate` tool hint into the batch prompt context while counting forbidden/unresolved model mentions and excluding rejected/archived assets, raw provider payloads, hidden prompts, and secret-like values. Staging prompt composer evidence remains open, deployed backend batch payload evidence remains open, and real provider result visibility remains open.
- `VF-2if`: `scripts/validate_stage1_result_placement_contract.py` now validates the local `FE-11` result placement contract across `web/lib/result-placement.ts`, `ResultPlacementEvidence`, `DevZenariClient.refreshBatchGenerationProgress`, Workspace canvas result-placement attributes, focused Vitest coverage, workspace smoke coverage, and `fixtures/stage1/result_placement/local_contract.json`. The local contract takes succeeded batch children with `asset_id`, `canvas_object_id`, and `trace_id`, creates idempotent `generated_layer` canvas nodes, links them from `node-brief`, adds active reusable Asset Library entries with `batch_child_provider_result` lineage, creates a canvas version snapshot, and keeps raw provider payloads out of projection. Staging backend result visibility, real provider asset bytes, rendered thumbnails/posters, signed URL retention evidence, and production security evidence remain open.
- `VF-2ifa`: `web/tests/stage1-prompt-result.spec.ts` now provides focused local browser coverage for the `FE-8`/`FE-9`/`FE-11` prompt-to-result workflow, including mention parsing for object/asset/brand/model/skill, duplicate and unresolved mention projection, prompt composer count/ratio/quality/model/tool payload attributes, CSRF-guarded batch create prompt-context preservation, mocked same-origin generated-client progress refresh, successful child result placement into canvas nodes and Asset Library entries, lineage/trace projection, and no raw provider payload projection. This is local Playwright contract evidence only; deployed-backend prompt payload evidence, staging Playwright result visibility, real provider bytes, signed URL/retention proof, and production security evidence remain open.
- `VF-2ig`: `scripts/validate_stage1_safety_export_state_contract.py` now validates the local `FE-12`/`FE-16` safety export product UI contract across `web/lib/safety-export-state.ts`, `SafetyExportStateEvidence`, `Workspace` package/export UI anchors, focused Vitest coverage, workspace smoke coverage, and `fixtures/stage1/safety_export_state/local_contract.json`. The local contract proves blocked exports expose QA/safety/admin-review counts and blocked reasons, retain manifest/provenance visibility, project no raw provider/safety payloads or secret-like values, and keep blocked download/share CTAs at zero while disabling share controls for blocked exports. Strict staging export review evidence remains open, and real signed URL download evidence, rendered export assets, object-retention evidence, and production security evidence remain open.
- `VF-2iga`: `web/tests/stage1-safety-export.spec.ts` now provides focused local browser coverage for the `FE-12`/`FE-16` safety export route, including safety-policy blocked exports, entitlement/QA blocked exports, admin-review counts, blocked reason projection, disabled share controls, suppressed download CTAs for blocked exports, and no raw provider/safety payload or secret-like projection. This is local Playwright contract evidence only; strict staging export review, real signed URL download, rendered export assets, object-retention cleanup, and production security evidence remain open.
- `VF-2ih`: `scripts/validate_stage1_rendered_export_asset_contract.py` now validates the local `FE-16`/`AS-10`/`AS-11` rendered export asset contract across `RenderedExportAssetEvidence`, `web/lib/dev-state.ts`, browser ZIP generation tests, Workspace export detail/download anchors, smoke artifacts, and `fixtures/stage1/rendered_export_asset/local_contract.json`. ZIP exports now map `assets/` to the rendered asset manifest at `assets/local-rendered-asset-manifest.json` and map manifest image/PDF output names such as `assets/square_social_ad.png` to deterministic local SVG payloads under `assets/rendered/`, with zero `assets/README.txt` placeholder payloads, safe path checks, no raw provider/safety payload projection, no secret-like projection, and explicit `open` status for staging signed URL and object retention evidence. This local contract does not prove real provider image bytes, backend PNG/SVG/PDF/PSD rendering, staging signed URL downloads, object retention cleanup, or production security evidence.
- `VF-2j`: `scripts/validate_stage1_asset_library_brandkit_contract.py` now validates the local `AS-8`/`AS-9` asset library and Brand Kit contract across `backend/internal/assets/library.go`, `backend/internal/brandkit`, OpenAPI asset-library and brand-kit schemas/routes, migration `backend/migrations/0019_stage1_asset_library_brand_kits.sql`, focused Go tests, local read/write API route handlers, tenant-scoped repository methods, safe projection tests, frontend picker/write-client anchors under `web/lib/asset-library-client.ts`, `web/lib/api-client.ts`, `web/components/workspace-app.tsx`, user-route smoke evidence, and `fixtures/stage1/asset_library_brandkit/local_contract.json`. The local contract covers tenant-scoped asset reuse, prompt attachment, canvas insertion, favorite/archive policy, private cross-project reuse denial, Brand Kit logo asset refs, palette hex validation, fonts, guidelines, source refs, project default bindings, active-only prompt context projection, principal-tenant read/write filtering, idempotent asset-library and Brand Kit writes, secret-like guideline/action/API projection redaction, and a local user picker/write UI for refresh, add asset, favorite, archive, create/update Brand Kit, set default Brand Kit, and package-as-reference behavior. Deployed staging runtime evidence and production security evidence remain open.
- `VF-2ja`: `web/tests/stage1-asset-brandkit.spec.ts` now provides focused local browser coverage for the `FE-13`/`FE-14` Asset Library and Brand Kit picker workflow, including operation contract projection, default Brand Kit summary, tenant/project asset rows, package-as-reference behavior, add-from-canvas asset creation, favorite/archive controls, Brand Kit create/update/set-default controls, and CSRF guard metadata for every write action. This is local Playwright contract evidence only; deployed API runtime, staging Asset Library/Brand Kit evidence, real provider asset bytes, signed URL/retention evidence, and production security evidence remain open.
- `VF-3`: `scripts/validate_stage1_provider_registry_contract.py` now validates provider registry/capability fixtures, provider strategy group fixtures, secret-ref policy, admin projection, user projection, OpenAI-compatible sandbox adapter wiring, audited registry create/update/delete/health-probe/test-call code anchors, audited strategy group create/update code anchors, strategy-group-driven batch routing code anchors, OpenAPI operation IDs, generated admin client operations, admin actions/UI controls, and SQL anchors. Staging provider sandbox runtime evidence is still open.
- `VF-3aa`: `admin/tests/stage1-admin-core-ops.spec.ts` now provides focused local browser coverage for the `AD-3`/`AD-4`/`AD-5` provider registry and provider strategy group management surface, including fixture fallback visibility, provider IDs and strategy group projection, create/update/test/probe/delete controls, secret-reference-only projection, disabled mutation controls when the live admin API is unavailable, production provider mode evidence, RBAC evidence/runtime/attempt sections, and no silent fallback copy. This is local admin Playwright contract evidence only; live deployed admin provider API, staging provider sandbox runtime, real provider test-call output, and production provider/claims evidence remain open.
- `VF-3a`: `scripts/validate_stage1_provider_sandbox_evidence.py` and `scripts/stage1_provider_sandbox_smoke.sh` now define the exact Stage 1 provider sandbox evidence contract. Contract-only validation is wired for local/repo checks. The smoke/validator reject localhost/private/non-HTTPS API URLs by default; local dev-port LLM runs must use explicit local-devport mode, now default to `ops/evidence/staging/local-devport/stage1-provider-sandbox.local-devport.json` plus `.ndjson`, and must be treated as debug evidence, not launch evidence. The strict validator still requires production-like staging evidence at `ops/evidence/staging/stage1-provider-sandbox.json` plus NDJSON results before this launch gate can close.
- `VF-3b`: `scripts/validate_stage1_provider_edit_adapter_contract.py` now validates the local `PR-6` provider edit adapter contract across `backend/internal/provider/adapters/edit`, the OpenAI-compatible capability declaration, provider registry fixture/user projection, focused Go tests, and `fixtures/stage1/provider_edit_adapter/local_contract.json`. The local contract covers remove-background/upscale/erase/expand, source-aligned mask input for erase/expand, result asset projection, request hashing, provider client wrapping, no raw provider payload projection/persistence, and secret-like input/result rejection. Real provider edit runtime, staging provider edit evidence, production security evidence, and deployed edit persistence remain open.
- `VF-3c`: `scripts/validate_stage1_provider_video_adapter_contract.py` now validates the local `PR-7` video provider adapter contract across `backend/internal/provider/adapters/video`, focused Go tests, and `fixtures/stage1/provider_video_adapter/local_contract.json`. The local contract covers duration/aspect/first-last-frame/status-polling/storage-result, video/poster result projection, provider client wrapping, request hashing, no raw provider payload projection/persistence, and secret-like input/payload/result rejection. Real provider video runtime, staging provider video evidence, production security evidence, and deployed video persistence remain open.
- `VF-4`: `scripts/stage1_stripe_staging_smoke.sh`, `scripts/validate_stage1_stripe_staging_evidence.py`, and `fixtures/stage1/stripe/stripe_staging_lifecycle_contract.json` now define the exact Stripe staging lifecycle evidence contract and canonical evidence generator. The smoke now resolves the target user's quota bucket from Postgres before the manual-credit quota projection step, so local/staging seed bucket IDs do not have to be hard-coded. The strict validator rejects localhost/private/non-HTTPS evidence by default; a local dev-port Stripe test-mode run only passes with explicit `--allow-local-devport` and is stored under `ops/evidence/staging/local-devport/` as debug evidence, not launch evidence.
- `VF-4a`: `scripts/validate_stage1_team_seat_billing_contract.py` now validates the `BL-6` team/seat billing backend/API contract, including `backend/internal/team`, `backend/internal/server` team routes, admin billing-link read/upsert/history routes, `backend/migrations/0014_stage1_team_seat_billing.sql`, OpenAPI/generated-client anchors, focused Go tests, reserved invite seat conversion, owner-removal denial, audit redaction anchors, `team_billing_links`, idempotent `team_seat_billing_syncs`, admin quota-page controls, and Stripe subscription-item quantity/proration sync anchors.
- `VF-4b`: `scripts/validate_stage1_admin_billing_ops_contract.py` now validates the `BL-8`/`AD-7` admin billing operations chain across backend repository/server routes, OpenAPI/generated admin client operation IDs, admin `/quota` server actions, visible manual-credit/refund-note/subscription-sync/account-lock forms, CSRF/idempotency/rationale anchors, and quota-page static tests.
- `VF-4c`: `scripts/validate_stage1_user_billing_invoice_contract.py` now validates the `BL-7` user billing invoice/receipt UI chain across backend `GET /api/v1/billing/invoices`, Stripe `hosted_invoice_url`/`invoice_pdf` mapping, OpenAPI/generated web client anchors, `web/lib/billing-client.ts`, `DevZenariClient.refreshBillingInvoices`, local invoice fixtures, the `stage1.invoice-receipt-product-ui` card, `Refresh Invoices` guard contracts, invoice/receipt link rows, web smoke tests, and browser security contract fixtures; invoice/receipt product UI now exists locally. Real Stripe invoice/receipt staging evidence remains open.
- `VF-4ca`: `web/tests/stage1-billing.spec.ts` now provides focused local browser coverage for the `FE-15`/`BL-7`/`BL-6` billing route, including checkout/portal/cancel CSRF contracts, invoice/receipt rows, team seat usage/entitlement/accept-invite contracts, safe team-seat billing projection attributes, and fallback click-state transitions when Playwright runs only the local web server. This is local browser contract evidence only; real Stripe staging invoice/receipt, checkout/webhook, team-seat proration, and production paid billing lifecycle evidence remain open.
- `VF-4cb`: `admin/tests/stage1-admin-core-ops.spec.ts` now also provides local browser coverage for the `AD-7` admin quota/team-seat/billing operations surface, including team seat usage cards, team billing-link projection, seat-sync rows, disabled team mutation controls under fixture fallback, admin billing operation CSRF/idempotency contract attributes, manual credit/refund note/subscription sync/account lock forms, local operation history rows, production paid billing lifecycle evidence, quota RBAC sections, and staging quota evidence visibility. This is local admin Playwright contract evidence only; real Stripe refund execution, deployed team-seat proration/sync evidence, staging checkout/webhook lifecycle, and production paid billing evidence remain open.
- `VF-4d`: `scripts/validate_stage1_stripe_lifecycle_reconciliation.py` now validates the local `BL-4`/`BL-5` Stripe lifecycle reconciliation contract across `StripeLifecycleReconciler`, webhook event summaries, subscription projection, admin refund-note/manual-credit summaries, quota transaction/bucket summaries, due quota reset invocation, replay idempotency flags, no raw webhook payload/secret projection, focused backend tests, and `fixtures/stage1/stripe_lifecycle/local_reconciliation.json`. This is local reconciliation contract evidence only; canonical `BL-9` staging Stripe checkout/webhook evidence remains open.
- `VF-5`: `scripts/validate_stage1_safety_qa_evidence.py`, `scripts/stage1_safety_qa_eval_smoke.sh`, and `fixtures/stage1/safety_qa_eval/local_contract.json` now define the exact Stage 1 safety/QA/eval evidence contract for batch blocked-child refunds, safety review reasons, edit-tool policy projection, asset-import blocking, export fail-closed behavior, admin review override audit, support-ticket redaction, and no secret/raw prompt/raw provider/raw safety payload exposure. Contract-only validation is wired for local/repo checks. The smoke/validator reject localhost/private/non-HTTPS API URLs by default and local dev-port probes must use explicit local-devport mode; strict validation still requires canonical production-like staging evidence at `ops/evidence/staging/stage1-safety-qa-eval.json` plus `ops/evidence/staging/stage1-safety-qa-eval.ndjson` before `QA-9` can close.
- `VF-5a`: `scripts/validate_stage1_safety_review_contract.py` now validates the local `AD-8` safety review admin chain across `GET /api/admin/v1/safety/reviews`, `POST /api/admin/v1/safety/reviews/{decision_id}/decision`, `backend/migrations/0016_stage1_safety_review_ops.sql`, tenant-scoped safe projections, CSRF/idempotency, audit-before-persistence, secret/rationale redaction, OpenAPI/generated admin client operations, admin `/safety` API fallback UI, static admin tests, and `fixtures/stage1/safety_review/local_contract.json`. It is wired into the default `scripts/repo_validate.sh` Stage 1 local contract baseline. This is local API-backed contract evidence only; deployed staging safety review evidence remains open and does not close `stage1-safety-qa-eval`.
- `VF-5b`: `scripts/validate_stage1_export_ops_contract.py` now validates the local `AD-10` export operations chain across `GET /api/admin/v1/exports`, `POST /api/admin/v1/exports/{export_id}/regenerate`, `POST /api/admin/v1/exports/{export_id}/override`, `POST /api/admin/v1/exports/cleanup`, `backend/migrations/0017_stage1_export_override_ops.sql`, tenant-scoped export ownership checks, CSRF/idempotency, audit-before-override-persistence, redacted rationale/metadata, fail-closed override outcome, OpenAPI/generated admin client operations, admin `/exports` live API fallback UI, server actions, static admin tests, and `fixtures/stage1/export_ops/local_contract.json`. This is local API-backed contract evidence only; deployed staging export override, signed URL, retention cleanup, and real rendered export evidence remain open.
- `VF-5c`: `scripts/validate_stage1_skill_eval_release_contract.py` now validates the local `AD-9` skill/eval release admin chain across `GET /api/admin/v1/skills`, `GET /api/admin/v1/skills/{skill_id}/versions`, `GET /api/admin/v1/eval/results`, `GET /api/admin/v1/eval/results/{result_id}/artifact`, tenant-or-global skill scoping, tenant-scoped eval result reads, latest eval release-gate projection, no eval rerun, safe eval summary/fixture projection redaction, audited admin artifact retrieval metadata, `skill_release:admin`, OpenAPI/generated admin client anchors, admin `/skills` and `/skills/releases` live API fallback UI, focused backend/admin tests, and `fixtures/stage1/skill_eval_release/local_contract.json`. This is local API-backed contract evidence only; deployed staging and production skill release/eval/canary evidence remain open.
- `VF-5d`: `scripts/validate_stage1_export_manifest_render_contract.py` now validates the local `AS-10`/`AS-11` export manifest/render contract across `backend/internal/export`, focused Go tests, and `fixtures/stage1/export_manifest_render/local_contract.json`. The local contract requires manifest, QA report, metadata, trace provenance, safety disclaimer, license/disclaimer refs, fail-closed gate evaluation, safe relative file paths, storage object keys without signed URL query/fragment, and blocks placeholder rendered assets from becoming finished exports; PSD output is limited to a layer manifest until a real PSD renderer exists. Real PNG/SVG/PDF rendering, real PSD rendering, signed URL staging evidence, object retention cleanup evidence, and production security evidence remain open.
- `VF-5e`: `scripts/validate_stage1_eval_skill_release_contract.py` now validates the local `QA-6`/`QA-7` eval and skillbook release contract across `backend/internal/eval`, `backend/internal/skillbook`, focused Go tests, and `fixtures/stage1/eval_skill_release/local_contract.json`. The local contract requires the Stage 1 eval suites for batch generation, provider routing, edit tools, export, billing/quota, and safety; stored eval results must carry deterministic runner hashes, source fixture digests, trace/export/QA/safety coverage, no critical safety regressions for pass, and read-without-rerun semantics. User-visible skill templates are projected only after review, eval, canary, rollback, and safety gates pass, and the user projection excludes internal prompt fragments, hidden policy, raw prompts, provider payloads, and safety payloads. This is local contract evidence only; staging and production skill release/eval/canary evidence remain open.
- `VF-6a`: `scripts/validate_stage1_release_readiness_contract.py` now validates the local `AD-14` release readiness admin panel contract across admin `/release`, `getStage1ReleaseReadiness`, typed aggregate evidence projections, Stage 1 staging runtime and production launch evidence paths, contract/strict validator/generator anchors, Stage 0 gate fixture and CI evidence tables, a read-only Evidence Closure Queue for quota replay/load/object retention/CI/production split gaps, safe projection policy fields, disabled manual go controls, static admin tests, and `fixtures/stage1/release_readiness/local_contract.json`. The panel reads validator evidence only and cannot clear staging, production, or Do-Not-Launch gates; strict staging/production evidence remains open.
- `VF-6b`: `scripts/validate_stage1_release_metadata_contract.py`, `scripts/generate_stage1_release_candidate_metadata.py`, `scripts/generate_stage1_release_metadata_preflight.py`, and `fixtures/stage1/release_metadata/local_contract.json` now define the release metadata preflight feeding `scripts/release_evidence_bundle_smoke.sh`. The release candidate metadata draft at `ops/release/stage1_release_candidate_metadata_draft.md` plus `ops/evidence/release/staging/stage1-release-candidate-metadata-draft.json` records the current repository SHA, migration list, config/env names, and explicit no-go conditions without reading local secrets or claiming strict evidence. The preflight centralizes `release_sha`, release notes, image refs, migration, config diff, observability, backup/restore, OP-7 load, rollback, and security scan evidence refs into `ops/evidence/release/staging/stage1-release-metadata-preflight.json`; missing or unverified slots keep `metadata_complete=false` and are projected into the release evidence bundle as blockers. This is release bundle input validation only and cannot clear Stage 1 staging runtime, production launch, or Do-Not-Launch gates.

## Frontend Gaps

- `FE-1`: User-facing brand text, browser metadata, legal/support copy, and billing copy now use `zenari.ai` in the active app surfaces; screenshot/staging evidence remains open.
- `FE-2`: Workspace state still depends on dev client contracts and needs a Stage 1 API-backed client boundary.
- `FE-3` to `FE-7`: A local canvas interaction contract now exists for `stage1.canvas-interaction-user-contract`, toolbar tools, zoom/fit, selected-node state, node move, layer hide/lock, and keyboard duplicate/delete, backed by `scripts/validate_stage1_canvas_interaction_contract.py`. The user workspace also exposes the local `stage1.edit-tools-mask-local-contract` panel for tool switching, mask kind/coverage controls, aligned mask evidence, and an `Apply Edit Tool` guard. Remaining open: real infinite-canvas editor/library behavior, richer object insertion/editing, real provider edit adapter behavior, and staging Playwright evidence.
- `FE-4`, `FE-5`, and `FE-6`: Object-shape rendering, toolbar controls, and layers-panel behavior are tracked through the local canvas interaction contract, `web/components/workspace-app.tsx` UI anchors, focused workspace tests, and `web/tests/stage1-canvas.spec.ts`. This is local browser/product contract coverage only; deployed persistence reads, real editor-library integration, screenshot evidence, and production-like staging Playwright evidence remain open.
- `FE-8` to `FE-11`: Prompt composer, mention parser, full batch progress UI, and result-to-canvas placement can now target the Stage 1 batch contract. `FE-8` now has a local prompt composer payload contract in `web/lib/prompt-context.ts`, with Workspace controls and batch creation wiring that project brief text, selected object chips, accepted reference assets, active reusable asset-library assets, Brand Kit, allowed model hints, requested count, aspect ratio, quality, and `image.generate` tool hint into `PromptComposerPayload`; `scripts/validate_stage1_prompt_composer_contract.py` validates this against `fixtures/stage1/prompt_composer/local_contract.json`. The user workspace has CSRF-guarded create/cancel/retry batch controls backed by dev state and generated API contracts, plus an authenticated workspace polling path that maps `getBatchGeneration`, `listBatchGenerationChildren`, and `getBatchGenerationProgress` into progress counts, retryable child counts, and result asset/canvas IDs. `FE-11` now has a local result placement contract in `web/lib/result-placement.ts`, validated by `scripts/validate_stage1_result_placement_contract.py`, that turns successful child results into `generated_layer` canvas nodes, Asset Library entries, brief-to-result edges, and a canvas version snapshot without projecting raw provider payloads. `FE-9` now has a local mention parser/picker contract in `web/lib/mentions.ts`, with Workspace Brief UI evidence for `@object`, `@asset`, `@brand`, `@skill`, and allowed `@model` mentions. `web/tests/stage1-batch-generation.spec.ts` adds local browser workflow coverage for create/progress/retry/cancel controls, progressbar state, and child-row visibility. Deployed-backend staging Playwright batch workflow coverage, SSE or production-grade polling evidence, staging prompt composer evidence, deployed backend batch payload evidence, real provider asset bytes/thumbnails/posters, signed URL retention evidence, and real result visibility remain open.
- `FE-12` to `FE-16`: Safety states, deeper billing/team/quota UI, and export panel still need more Stage 1 data contracts and tests. The user workspace now exposes a local Asset Library and Brand Kit picker/write panel with `stage1.asset-library-brandkit-user-picker` evidence, a generated-client-backed `Refresh Assets` read contract for `listAssetLibrary`, `listBrandKits`, and `getProjectDefaultBrandKit`, idempotent write contracts for `createAssetLibraryEntry`, `updateAssetLibraryEntry`, `createBrandKit`, `updateBrandKit`, and `setProjectDefaultBrandKit`, default Brand Kit summary, asset reuse rows, and local package-as-reference behavior. The user workspace billing page exposes a CSRF-guarded Team Seats panel for seat usage, entitlement refresh, pending invite context, and accept-invite flow backed by generated API contracts. It also exposes a local invoice/receipt card backed by `GET /billing/invoices`, a guarded `Refresh Invoices` control, invoice and receipt links, local fallback fixtures, and browser smoke contract evidence. Real API-backed staging evidence, real Stripe invoice/receipt artifacts, and broader team self-service management remain open.

## Admin Gaps

- `AD-1`: Admin visible brand text now uses `zenari.ai` in active surfaces verified by typecheck/text scan; staging screenshot evidence remains open.
- `AD-2`: Admin auth/session/RBAC must remain separate from user auth.
- `AD-3`: Provider list now has a real admin read API target through `GET /api/admin/v1/providers/registry`, backed by provider registry persistence and protected by `provider_routing:admin`; admin provider page consumes that API when available and falls back to explicit fixture data when the local backend/admin session is not available.
- `AD-4`: Provider status/routing write now has `PATCH /api/admin/v1/providers/registry/{provider_id}` with RBAC, CSRF/idempotency, rationale validation, before/after response, audit logging, and an admin provider page save form for status, weight, canary, concurrency, fallback, kill switch, secret reference, capability, batch limits, and cost fields. Provider create/delete now also has audited `POST /api/admin/v1/providers/registry` and `DELETE /api/admin/v1/providers/registry/{provider_id}` with admin UI forms, idempotency, CSRF, non-secret rationale validation, safe secret-reference handling, and before/delete projection evidence. Provider strategy groups now have `GET/POST/PATCH /api/admin/v1/providers/strategy-groups`, additive SQL tables, OpenAPI/generated admin client operations, admin create/update forms for selection policy, fallback, member weights, canary, concurrency, fallback rank, and kill switch, plus audited rationale-bound mutations.
- `AD-5`: Provider health probe and sandbox test-call APIs, admin forms, CSRF/idempotency contracts, audit actions, generated admin client operations, fixture, validator, and admin test anchors now exist. Health probes call the configured provider client status path and persist a safe registry health snapshot for routing/canary decisions. Staging sandbox runtime evidence and audit drill links remain open.
- `AD-6`: The admin `/queues` page now exposes a Stage 1 batch operations dashboard that prefers live `GET /api/admin/v1/batch-generations/queue-runtime` and `GET /api/admin/v1/batch-generation-children` responses, falling back to local contract fixtures when the admin backend/session is unavailable. The backend read API is tenant-scoped from the admin principal, protected by `audit:read`, backed by safe batch repository projections that do not select prompt text or return raw failure/provider metadata, and covered by focused server/task tests plus OpenAPI/generated admin-client anchors. The dashboard shows batch aggregate status, child task status, worker claim timeout/drain policy, provider strategy group and provider/model concurrency, retry budget, dead-letter state, safety review reason, quota estimate/commit/refund, result asset/canvas placement, provider usage refs, retry-attempt scoped idempotency keys, evidence refs, and audit refs. Real deployed admin batch API/runtime evidence, staging queue contention, and live worker restart/drain evidence remain open.
- `AD-7`: The quota page now exposes API-backed Team Seat Operations controls for team create, billable seat invite, member removal, admin seat-usage review, Stripe subscription-item billing-link binding, and recent `seat-syncs` reconciliation history when the admin backend is available, with fixture fallback otherwise. It also exposes audited admin billing operation forms for manual credit, refund note, subscription sync, and account lock, posting to the backend admin billing API with same-site CSRF, idempotency, ticket metadata, and rationale fields. Real Stripe refund execution, quota reset automation, and staging evidence remain open.
- `AD-8`: The admin safety page now prefers live `GET /api/admin/v1/safety/reviews` for tenant-scoped safety review queue rows and falls back to fixtures only when the admin backend/session is unavailable. Review decisions can be submitted through `POST /api/admin/v1/safety/reviews/{decision_id}/decision`, protected by `safety_rule:admin`, same-site CSRF, `Idempotency-Key`, non-secret rationale validation, audit-before-persistence, and `safety_review_decisions` idempotency records. Projections expose safety decision IDs, review status, required evidence refs, audit refs, user-visible outcome, and explicit no-raw-prompt/provider/safety/secret flags. Staging runtime review evidence remains open.
- `AD-9`: Eval/skill release screens now prefer live `GET /api/admin/v1/skills`, `GET /api/admin/v1/skills/{skill_id}/versions`, and `GET /api/admin/v1/eval/results` responses and fall back to fixtures only when the admin backend/session is unavailable. Backend read routes are protected by `skill_release:admin`, derive release-gate canary/active eligibility from latest tenant-scoped `eval_results`, return stored eval result projections without re-running eval suites, redact summary/fixture/runner/storage metadata, and expose `GET /api/admin/v1/eval/results/{result_id}/artifact` as audited admin retrieval metadata with direct object access disabled. Deployed staging and production skill release/eval/canary evidence remains open.
- `AD-10`: Export operations now have a local API-backed admin chain for export listing, regeneration, override decision recording, cleanup submission, signed URL presence, retention, blocked reasons, manifest, QA report, provenance, trace, CSRF/idempotency, audit-before-persistence, and fixture fallback. Real deployed staging evidence for override workflows, signed URL validity, retention cleanup, object storage cleanup, and real rendered export files remains open.
- `AD-11` to `AD-13`: Operations, support, and audit panels must continue to read validator evidence only.
- `AD-12`: Support/admin deletion governance is tracked by `scripts/validate_stage1_support_admin_deletion_governance_contract.py`, `fixtures/stage1/support_admin_deletion_governance/local_contract.json`, and the admin `/support` panel. It exposes trace/export/billing/asset links, retained evidence, second-review state, blocked gate checks, and denied raw projection fields without deletion mutation controls; strict staging legal/support, production legal/support, production security, and paid billing evidence remain open.
- `AD-14`: Release readiness now has a local read-only admin `/release` panel that reads Stage 1 aggregate staging runtime and production launch evidence, Stage 0 gate fixtures, CI evidence, validator commands, safe projection policy, blockers, and Do-Not-Launch conditions through `getStage1ReleaseReadiness`. It has no manual go controls and only reflects validator evidence; strict staging/production evidence remains open.

## Backend Gaps

- `BE-1`: Config now has Stage 1 brand, Stripe, local devport fallbacks, and OpenAI-compatible LLM entries. The backend no-env local defaults align with the reserved Zenari host ports for Postgres `26432`, Redis `26379`, and MinIO API `26900`; broader provider, batch, queue, and observability required/default rules remain to be expanded.
- `BE-2` to `BE-4`: Secret redaction, auth/RBAC separation, and immutable audit need Stage 1 paths added.
- `BE-3`: User/admin auth and RBAC separation now has local coverage through `backend/internal/auth`, server middleware/tests, generated-client CSRF/session checks, and admin route denial assertions. Real identity-provider wiring, deployed session refresh/logout expiry, and production audit evidence remain open.
- `BE-5`: OpenAPI Stage 1 contract now includes batch generation endpoints, provider admin registry/status routing, checkout/subscription operations, team/seat usage/invite/admin operations, Stage 1 canvas/asset schemas, and asset-library/Brand Kit read/write operations. Deeper billing lifecycle contracts remain incomplete.
- `BE-6` to `BE-9`: Local canvas object, visual asset, and tenant isolation packages now exist in `backend/internal/canvas` and `backend/internal/assets`, with Stage 1 object/asset type enums, transforms, z-index/locked/hidden state, storage refs, thumbnail refs, lineage refs, tenant-scoped projections, secret/raw-payload rejection, and tenant-predicate SQL anchors. Tenant FK/index migrations and result-sink persistence anchors are covered by `scripts/validate_stage1_canvas_assets_contract.py`. Real deployed API read/write persistence, UI placement, signed URL/retention behavior, and staging runtime evidence remain open.
- `BE-7`: Canvas object/version/frame/group persistence is represented by additive migrations, `backend/internal/canvas` model/version tests, and `PostgresBatchResultSink` persistence anchors. Full API-backed canvas read/write runtime, rollback evidence, and deployed migration proof remain open.
- `BE-10` to `BE-11`: Local PromptContextPayload and TraceProjection contracts now exist in `backend/internal/trace`; `PostgresBatchResultSink` writes user/export-safe trace projections with prompt hashes plus selected objects, assets, brand, model hints, and tool hints while keeping raw prompt/provider/safety payload and secrets out of user/export projection. Staging trace runtime evidence and production security evidence remain open.
- `BE-12`: Local support ticket linking/redaction now exists through `backend/internal/support` plus the stage0 repository path. Tickets require project, task, batch, trace, asset, export, quota bucket, and billing reference links; body and metadata are redacted before persistence and again on admin list projection; OpenAPI exposes the required support evidence fields. Deployed support workflow and production security evidence remain open.
- `BE-13`: Local request-time rate-limit and spend-cap enforcement now exists in `backend/internal/ratelimit` with user, tenant, provider, and admin-action scopes; fixed-window MemoryStore for local tests; RedisStore for production-like atomic counters/reservations; explainable `rate_limit_exceeded`, `daily_spend_cap_exceeded`, and `provider_kill_switch_enabled` denial decisions; safe `rate_limit.denied` audit metadata; config/env defaults; non-local `RATELIMIT_STORE=redis` validation; and server wiring for batch create, provider health/test-call, and admin billing mutations. Redis-backed staging runtime evidence and production security launch evidence remain open.
- `BE-14`: Local API error taxonomy now exists in `backend/internal/api`, and the shared server error envelope projects `taxonomy`, `retryable`, and `blocked` while preserving `code`, `message`, `request_id`, `details`, and `field_errors`. OpenAPI now documents `ErrorTaxonomy` and the blueprint states `retryable`, `blocked`, `quota_insufficient`, `provider_unavailable`, and `review_required`; frontend staging error-state evidence remains open.

## Worker And Batch Gaps

- `WK-1`: Batch request and child task Go contracts, additive SQL tables, replay fixtures, validator, repository layer, and child completion/failure persistence now exist. Full worker loop integration is still open.
- `WK-2`: Batch API endpoints, OpenAPI contract, generated web/admin clients, backend routing, and focused server/task/app tests now exist for create, get, list children, progress, cancel, and retry child task.
- `WK-3`: Worker-side fan-out claim scheduler now exists as a backend `ClaimRunnableChildren` contract with tenant/provider/provider-model concurrency policy, allowed provider/model/tool filtering, worker claim metadata, fixture evidence, and task tests. A post-claim `BatchChildExecutor` execution unit now builds provider requests, invokes a resolved provider client, records provider usage logs through a recorder interface, requires a result sink before success, and routes success/failure through repository completion methods. A configurable `BatchRunner` loop can now claim and execute children from `cmd/worker` when `WORKER_BATCH_ENABLED=true`.
- `WK-4` to `WK-6`: Tenant/provider/model concurrency claim is present. Batch create can now reserve estimated quota against a user quota bucket through `PostgresBatchQuotaLedger`, child success/failure can commit actual usage plus refund the remainder through quota transactions, retryable provider failures can be requeued without refunding reserved quota, exhausted/non-retryable failures are dead-lettered with final refunds, manual retry re-reserves previously refunded quota, retry-attempt scoped quota idempotency prevents stale terminal refund keys from swallowing manual retry commit/refund, and local runtime replay evidence now covers provider usage debit/credit adjustments. Staging validation remains incomplete before real paid batch generation is enabled.
- `WK-5`: Retry policy and dead-letter handling are locally covered by `backend/internal/task/batch_retry.go`, retry/dead-letter quota fixtures, batch quota reconciliation validators, and runtime replay tests. Deployed provider retry classification, trace retention, and quota idempotency evidence remain open.
- `WK-7` to `WK-9`: Progress and cancellation API surfaces exist, and the user workspace now has a local API-backed polling contract that refreshes batch aggregate, children, progress counts, retryable failures, and result asset/canvas IDs when a queued/running batch is visible to an authenticated user. Success completion now requires asset/canvas IDs returned by a result sink, and `PostgresBatchResultSink` writes object-store manifest refs, thumbnail refs, linked `object_metadata`, `assets`, `canvas_nodes`, lineage metadata, and user-visible trace projections without raw provider output. Realtime SSE or production polling cadence evidence, provider cancel propagation, real rendered provider image bytes, rendered thumbnails/posters, richer asset lineage, signed URL/retention evidence, and staging runtime evidence remain open.
- `WK-8`: Cancel batch/child task behavior is locally represented in batch fixtures, task state-machine tests, and API cancel/retry surfaces. Provider-native cancel handoff, refund reconciliation against live worker state, and staging cancel/retry evidence remain open.
- `WK-10`: Local pre-provider batch safety gate now exists. `BatchChildExecutor` evaluates `BatchSafetyGate` after loading the batch and before resolving/invoking a provider client; denied children call `BlockChildForReview`, persist `status=blocked` plus `review_reason`, refund remaining reserved quota through `RefundBatchQuota`, record safe metadata such as `safety_gate_blocked` and `provider_invoked=false`, and avoid raw prompt/provider/safety payload persistence. Focused task and worker tests cover executor, repository SQL/rollback, and runner behavior. Strict staging proof remains open until canonical `stage1-safety-qa-eval` evidence shows a real deployed batch child blocked before provider invocation.
- `WK-11`: Local worker idempotency/drain now covers claim timeout leases, expired unaccounted running-child requeue before concurrency counts, claim metadata (`claim_expires_at`, `claim_attempt`), shutdown drain that stops new batch claims, separate `WORKER_BATCH_CLAIM_TIMEOUT` config/default/validation, and retry-attempt-stable provider idempotency keys. Focused task/worker/config/cmd tests and `scripts/validate_stage1_batch_generation_contract.py` cover these local contracts. Strict staging proof remains open until a deployed worker restart/drain run proves completed children are not duplicated and expired in-flight unaccounted children are safely reclaimed.
- `WK-12`: Replayable batch fixtures exist for single, 4 variants, 20 variants, partial failure, cancel, and quota insufficient; they still need real runtime replay against API/worker.

## Provider Gaps

- `PR-1`: Provider capability schema now includes batch, seed, aspect, quality, cancel, and cost fields with Go validation.
- `PR-2`: Provider registry contract, fixture, validator, additive SQL tables, Postgres read repository, OpenAPI admin list/create/update/delete/health-probe/test-call operations, `GET/POST/PATCH/DELETE /api/admin/v1/providers/registry`, audited status/routing/secret-ref/capability/cost write, audited provider creation/deletion, audited health refresh, and deterministic admin-only sandbox test-call now exist. Staging sandbox evidence remains open.
- `PR-3`: Provider strategy groups now make routing policy admin-controlled and explainable through persisted group membership, tool surface, selection policy, fallback provider IDs, member weight/canary/concurrency/fallback-rank settings, kill switch state, audited admin create/update APIs, OpenAPI/generated client contracts, admin UI controls, and fixture-backed validator coverage. Batch creation now injects the provider registry strategy reader in server and worker runtimes, applies weighted/priority/canary/failover/kill-switch provider selection, and writes routing strategy group, policy, selection reason, fallback, and considered-provider metadata onto batch children for trace projection. Real staging traces and provider sandbox evidence remain open.
- `PR-4`: Dev provider must stay non-production and never close production provider evidence.
- `PR-5`: A first OpenAI-compatible sandbox adapter now exists for `zenari-image-sandbox`; the worker uses it only when `LLM_PROVIDER=openai-compatible` and `LLM_ENABLE_LIVE_CALLS=true`, otherwise local execution stays on the deterministic dev provider. The Stage 1 provider sandbox smoke/validator contract now requires an OpenAI-compatible `/models` health probe, admin sandbox test-call, batch worker runtime, persisted asset/canvas IDs, provider usage, no secret/raw prompt/raw provider payload exposure, and production-like staging API URL semantics by default. Canonical staging sandbox evidence, generated asset quality evidence, and real provider invoice/usage export reconciliation remain open before launch.
- `PR-6`: A local provider edit adapter contract now exists in `backend/internal/provider/adapters/edit`, backed by `scripts/validate_stage1_provider_edit_adapter_contract.py` and `fixtures/stage1/provider_edit_adapter/local_contract.json`. It wraps `provider.Client`, builds `image.edit` requests for remove-background/upscale/erase/expand, requires aligned masks for erase/expand, projects safe result assets, and keeps raw provider payloads out of projection/persistence. Real provider edit runtime, staging provider edit evidence, API-backed edit persistence, and production security evidence remain open.
- `PR-7`: A local provider video adapter contract now exists in `backend/internal/provider/adapters/video`, backed by `scripts/validate_stage1_provider_video_adapter_contract.py` and `fixtures/stage1/provider_video_adapter/local_contract.json`. It wraps `provider.Client`, builds `video.generate` requests with duration/aspect/first-last frame inputs, provides `video.status` polling projection, projects safe video/poster storage results, and keeps raw provider payloads out of projection/persistence. Real provider video runtime, staging provider video evidence, API-backed video persistence, and production security evidence remain open.
- `PR-8`: Provider health probing now exists as an audited admin operation at `POST /api/admin/v1/providers/registry/{provider_id}/health-probe`, backed by provider client `Status()` probes, safe `provider_registry.health` persistence, OpenAPI/generated admin client contracts, admin UI controls, and provider registry validator coverage. Real staging probe evidence remains open.
- `PR-9` and `PR-10`: Provider usage logging is now available to the batch child executor through a recorder interface, and `ProviderCostReconciler` can join `provider_usage_logs` to `provider_model_capabilities`, call `QuotaRepository.ReconcileProviderUsage`, mark provider-cost metadata, flag spend-cap breaches/outliers, and route missing quota idempotency to manual review. Staging provider sandbox evidence plus real provider invoice/usage export reconciliation remain open.

## Asset And Export Gaps

- `AS-1` to `AS-3`: Batch result sink can now require object storage, persist tenant-scoped result manifest and thumbnail manifest objects, write generated-image plus thumbnail `object_metadata` rows from object-store returned bucket/key/checksum/size, link `assets` and `canvas_nodes`, and record storage refs, thumbnail refs, lineage, and trace projections while keeping raw provider output out of persisted JSON. Local `VisualAsset` and `CanvasObject` contracts now validate these refs and tenant-safe projections. Real provider image bytes, rendered thumbnails/posters, richer asset lineage, signed URL/retention proof, and staging runtime evidence still need stable Stage 1 evidence.
- `AS-2`: Thumbnail pipeline tracking currently proves safe thumbnail/storage references, failed-placeholder rejection, video poster projection requirements, and no credential/query/fragment leakage through local asset and batch-result validators. Actual provider thumbnail bytes, video poster generation, and staging signed URL thumbnail access remain open.
- `AS-4` and `AS-5`: A local canvas versioning contract now exists for backend snapshot/diff/restore semantics and frontend `stage1.canvas-version-history` preview/restore attributes, backed by `scripts/validate_stage1_canvas_versioning_contract.py`. Remaining open: staging Playwright evidence, real collaborative conflict runtime, real editor-library integration, deployed persistence/runtime evidence, and richer version history preview/diff UX.
- `AS-6` and `AS-7`: A local edit tools/mask contract now exists for backend validation/revision semantics and frontend `stage1.edit-tools-mask-local-contract` UI attributes, backed by `scripts/validate_stage1_edit_tools_mask_contract.py`. A separate local `PR-6` provider edit adapter contract now covers the backend adapter boundary for remove-background/upscale/erase/expand. Remaining open: real provider edit runtime, API-backed edit persistence, staging Playwright evidence, production security evidence, and richer editor-library masking behavior.
- `AS-8` and `AS-9`: Local asset library and Brand Kit contracts now exist. `backend/internal/assets/library.go` covers tenant-scoped library entries, visibility, favorite/archive, reusable project policy, prompt attachment, canvas insertion, and action metadata redaction. `backend/internal/brandkit` covers logo asset refs, palette, fonts, guidelines, source refs, project bindings, project default lookup, and active-only prompt context projection. Migration `0019_stage1_asset_library_brand_kits.sql` adds `asset_library_entries` and `brand_kits` with tenant indexes/FKs. OpenAPI exposes `listAssetLibrary`, `createAssetLibraryEntry`, `updateAssetLibraryEntry`, `listBrandKits`, `createBrandKit`, `updateBrandKit`, `getProjectDefaultBrandKit`, and `setProjectDefaultBrandKit`, and backend wires local read/write API handlers for the matching `/api/v1/assets/library`, `/api/v1/brand-kits`, and `/api/v1/projects/{project_id}/brand-kit-default` routes through principal-tenant repository queries/mutations with idempotency and redacted projections. The user workspace now has a local picker/write client boundary for those eight operations, default Brand Kit display, reusable asset rows, refresh evidence, add/favorite/archive asset actions, create/update/default Brand Kit actions, and package-as-reference behavior. Deployed staging API runtime and production security evidence remain open.
- `AS-10` and `AS-11`: A local export manifest/render contract now exists in `backend/internal/export`, backed by `scripts/validate_stage1_export_manifest_render_contract.py` and `fixtures/stage1/export_manifest_render/local_contract.json`. It requires manifest/QA/metadata/trace/safety disclaimer files, trace provenance, license/disclaimer refs, fail-closed gate evaluation, safe storage keys, ZIP entry planning, PSD layer-manifest-only output, and blocks placeholder rendered assets from becoming finished exports. Real PNG/SVG/PDF rendering, real PSD rendering, signed URL staging evidence, object retention cleanup evidence, and production security evidence remain open.
- `AS-12`: Staging object retention/cleanup exact evidence remains a launch blocker; local export manifest/render and admin export ops contracts do not close signed URL validity, real object retention cleanup, or staging object retention cleanup gates.

## Billing Gaps

- `BL-1`: Stripe config and sandbox selftest are the current baseline; product UI and backend workflows still need Stage 1 completion.
- `BL-2`: Stripe checkout adapter, unit tests, authenticated backend checkout route, OpenAPI operation, generated client contract, and web checkout client now cover session creation form payload, metadata, idempotency key, Stripe API headers, same-site CSRF, and test-mode livemode rejection. Staging checkout evidence remains open.
- `BL-3`: Stripe webhook adapter, event store, subscription sync, signature/timestamp/livemode/idempotency tests, and browser-CSRF-bypassed webhook route now exist. Staging webhook replay evidence remains open.
- `BL-4`: Subscription lifecycle now has a local `StripeLifecycleReconciler` contract that summarizes checkout completion, invoice paid, payment failed, cancellation, subscription statuses, and webhook replay idempotency from stored Stripe event/subscription rows without projecting raw payload or secrets. Broader product handling for incomplete states and canonical staging lifecycle evidence remain open.
- `BL-5`: Quota ledger now has local provider usage reconciliation through `QuotaRepository.ReconcileProviderUsage`, provider-cost reconciliation through `ProviderCostReconciler`, and Stripe lifecycle reconciliation that verifies refund/credit admin operations, quota credit transactions, quota projection validity, and due quota reset invocation. Real provider spend exports, deployed Stripe refund execution, and staging quota reset/reconciliation evidence remain open.
- `BL-6`: The team/seat billing backend plus API/OpenAPI/generated-client contract now exists with `backend/internal/team`, `backend/internal/server/team_context.go`, user routes for seat usage/entitlement/invite acceptance, admin routes for team create/invite/member removal/admin seat usage, admin billing-link read/upsert/history routes under `/api/admin/v1/team-seat-ops/{team_id}`, migration `backend/migrations/0014_stage1_team_seat_billing.sql`, focused Go coverage, OpenAPI schemas, regenerated web/admin clients, user billing-page Team Seats UI for seat usage, entitlement, pending invite acceptance, minimal web CSRF guard evidence, admin quota-page Team Seat Operations controls, admin quota-page Team Billing Link controls, and `scripts/validate_stage1_team_seat_billing_contract.py`. It covers owner/member roles, idempotent invites, pending-invite seat reservation, invite acceptance without double-counting reserved seats, member removal denial for owners, seat entitlement projection, audit-before-mutation for admin operations, tenant scoping, audit metadata redaction, runtime team service and billing manager injection, `team_billing_links`, one-active-link enforcement, idempotent `team_seat_billing_syncs`, and Stripe subscription-item quantity/proration sync after invite/accept/remove seat changes. Real Stripe proration execution evidence and real API/UI/staging evidence remain open.
- `BL-7`: User checkout, quota, subscription, portal, cancel-at-period-end, and invoice-list API/client contracts are now wired for `POST /billing/checkout`, `GET /quota`, `GET /billing/subscription`, `POST /billing/portal`, `POST /billing/subscription/cancel`, and `GET /billing/invoices`. Backend routes use authenticated tenant/user context and stored Stripe provider refs; Stripe invoice mapping now projects `hosted_invoice_url` as the invoice link and `invoice_pdf` as the receipt link while rejecting `livemode=true` responses in test mode. Web billing client coverage proves CSRF/idempotency behavior, and the local workspace exposes guarded billing controls for checkout, portal, cancel, quota/subscription display, and a `stage1.invoice-receipt-product-ui` invoice/receipt card with `Refresh Invoices`, invoice rows, and Invoice/Receipt links. Staging Stripe evidence, production UI polish, refund/credit, quota reset, team seats, and real Stripe invoice/receipt artifact evidence remain open.
- `BL-8`: Admin billing ops API/backend/UI contract now exists for `POST /billing/manual-credit`, `POST /billing/refund-note`, `POST /billing/subscription-sync`, and `POST /billing/account-lock`. Backend coverage proves RBAC, audit-before-mutation, idempotency, redaction, tenant scoping, account-lock projection, and manual quota credit through the ledger; admin `/quota` now exposes manual credit, refund note, subscription sync, and account lock forms with CSRF/idempotency/rationale/ticket metadata anchors plus recent fixture operation rows. Real Stripe refund execution, quota reset automation, and staging evidence remain open.
- `BL-9`: Stripe staging lifecycle smoke/validator contract now exists for test checkout, webhook replay, `livemode=false`, paid/past_due/cancel/refund-credit, invoice/receipt visibility, and quota/account projection. The smoke can now autodetect the current user's quota bucket and a local dev-port Stripe test-mode run passed the explicit local-devport validation path across all ten scenarios. Deployed production-like staging evidence is still needed before aggregate runtime launch gates should be treated as closed.
- `BL-10`: Production billing evidence remains open. `scripts/validate_stage1_production_billing_evidence.py` now validates the exact paid billing lifecycle contract across `ops/evidence/production/billing-lifecycle.json`, `ops/evidence/production/billing-refund-credit-webhook.json`, the Stage 1 production aggregate contract, Stripe staging lifecycle validator anchors, local Stripe lifecycle reconciliation, admin billing ops, team seat billing, user invoice/receipt UI, full release SHA matching, live/test separation, production `livemode=true` Stripe artifact IDs with no test/sandbox/dev fixture ids, paid checkout, active/past_due/cancel lifecycle, team seat subscription-item sync with proration behavior and idempotency proof, hosted invoice/PDF receipt visibility, refund/credit, quota reset, webhook replay idempotency, failed-export refund, quota projection, safe projection flags, and no invite/comp-only/deferred/blocked/local/dry-run/secret/raw-payload markers. Contract-only validation is wired into `scripts/repo_validate.sh`; strict production paid billing evidence remains open until real paid production runs replace the current deferred comp-only no-go evidence.
- `BL-11`: Provider-cost reconciliation now has a local contract validator and backend service, but launch still requires real provider invoice/billing-period spend exports, staging quota transaction replay, and provider usage export reconciliation.

## QA And Safety Gaps

- `QA-1` to `QA-2`: Stage 1 safety policy and runtime enforcement now have a local evidence contract that reuses the Stage 0 safety enforcement validator and requires coverage across batch, edit, import, export, support, and admin review surfaces. The staging smoke supports dev identity headers only in explicitly local dev-port mode; runtime production-like staging proof remains open.
- `QA-3` to `QA-5`: Image QA, export fail-closed gates, and admin review now have a Stage 1 evidence validator contract tied to the existing Stage 0 QA result, export eligibility, and export override validators. Real staging asset/export/admin-review evidence remains open.
- `QA-4`: Export fail-closed behavior is locally enforced through `backend/internal/export`, manifest/render validators, safety export state coverage, and browser assertions that blocked exports suppress download/share actions. Strict staging still must prove QA failed, safety blocked, missing provenance, and missing manifest cases cannot produce downloadable signed URLs or export packages.
- `QA-6` and `QA-7`: Eval suites and user-visible skill templates now have a local backend release/eval/canary contract through `backend/internal/eval`, `backend/internal/skillbook`, `fixtures/stage1/eval_skill_release/local_contract.json`, and `scripts/validate_stage1_eval_skill_release_contract.py`. Strict staging and production skill release/eval/canary evidence remain open.
- `QA-8`: Local security scan coverage contract now exists through `scripts/security_scan_smoke.sh`, `scripts/validate_stage1_security_scan_contract.py`, and `fixtures/stage1/security_scan/local_contract.json`. It requires z.ai/OpenAI-compatible key shape scanning, Stripe key/webhook shape scanning, `web`/`admin` `npm audit --audit-level=moderate` reports, and the closed release image scan set `zenari-stage0-backend`、`zenari-stage0-web`、`zenari-stage0-admin`; backend runtime-worker/crawler/migrate coverage is proven through the backend image, and `manager` remains legacy local-only. It also covers provider/edit/video raw-payload containment, Stripe live/test and webhook verification anchors, batch result redaction/projection, asset signed-URL rejection, support ticket redaction, export manifest fail-closed checks, trace forbidden-field checks, rate-limit raw-secret projection guards, and Go 1.26.4 toolchain pinning for called-symbol `govulncheck` coverage. `scripts/validate_stage1_production_security_launch_evidence.py` now also defines the strict OP-13 production security launch evidence shape across secure cookies, CSRF, redaction, admin privacy, provider key containment, Stripe live/test separation, rate limit/spend cap, CSP, RBAC/tenant isolation, and audit refs. Strict staging security scan evidence and strict Production security launch evidence remain open; the current production security file is still check-level evidence with blockers preserved.
- `QA-9`: Strict staging safety/QA/eval evidence remains open; the strict `VF-5` validator intentionally fails until canonical `stage1-safety-qa-eval` evidence is generated from production-like staging. Localhost/private dev-port evidence requires explicit local-devport validation and must not close staging gates.

## Ops And Release Gaps

- `OP-1`: `.github/workflows/stage0-rev2-ci.yml` is now installed and extends the Stage 0 Rev2 draft with Stage 1 pre-launch contract validators plus Stripe sandbox selftest wiring. This closes only the installed workflow file evidence; exact PR/main run, Playwright smoke, and Docker image build evidence remain open.
- `OP-2` to `OP-4`: Exact CI evidence files remain open. `scripts/validate_stage1_ci_exact_evidence.py` now defines the strict contract across `ops/evidence/ci/stage0-rev2-pr-main-run.json`, `ops/evidence/ci/stage0-rev2-playwright-smoke.json`, `ops/evidence/ci/stage0-rev2-docker-image-build.json`, the installed GitHub Actions workflow, Stage 0 CI runtime evidence anchors, Stage 1 production aggregate CI dependencies, PR/main trigger semantics, Playwright user/admin/billing/batch/canvas coverage, Docker web/admin/backend image digests, full release SHA matching, gate-impact closure, safe projection fields, and default strict failure unless all three canonical CI files are strict-pass. Worker remains a backend command/runtime responsibility covered by backend build/tests and is not a standalone release image; manager is not a Stage 1 release blocker. The Docker workflow now uses `scripts/docker_build_smoke.sh` plus `scripts/write_stage1_ci_docker_evidence.py`; the validator checks the three release image rows and exact CI artifact shape in the evidence writer. Real installed workflow run artifacts remain required before the CI gate can close.
- `OP-5` and `OP-6`: Staging object cleanup and strict aggregate Stage 1 runtime evidence remain open. The `VF-6` aggregate staging runtime contract and `scripts/generate_stage1_staging_runtime_evidence.py` now exist; the generator can summarize current blockers into `ops/evidence/staging/stage1-runtime.json` and `.ndjson`, but it only emits `pass`/`go` after exact child evidence and strict child validators pass.
- `OP-7` to `OP-9`: Load, observability, and backup/restore staging evidence remain open.
- `OP-8`: Observability and incident dashboard coverage is tracked through `ops/observability`, `scripts/observability_smoke.sh`, `scripts/staging_observability_backup_load_smoke.sh`, `scripts/validate_stage1_operations_incident_runbook_contract.py`, and admin operations UI anchors. Imported staging dashboards, alert firing evidence, trace/metric correlation for batch/provider/quota/Stripe/export flows, and production incident response evidence remain open.
- `OP-10` and `OP-11`: Production backup/restore and rollback/incident/post-deploy exact evidence remain open. `scripts/validate_stage1_production_backup_rollback_evidence.py` now validates the exact evidence contract across `ops/evidence/production/backup-restore.json`, `ops/evidence/production/rollback-incident-post-deploy-smoke.json`, `scripts/production_backup_rollback_split_smoke.sh`, Stage 0 split preflight semantics, the Stage 1 production aggregate contract, full release SHA matching, required production backup/restore sections, rollback/incident/post-deploy sections including `backend_runtime_worker_rollback` on the backend release image with `runtime-worker` target and `/app/worker` entrypoint, CI/private-beta/staging dependency refs, safe projection flags, and no blocked/local/dry-run/secret/raw-payload markers. Contract-only validation is wired into `scripts/repo_validate.sh`; strict production exact split evidence remains open until real production backup and rollback runs generate the canonical files.
- `OP-12` and `OP-13`: Legal/support visibility and production security launch checks remain open. `scripts/validate_stage1_production_legal_support_evidence.py` now defines the production legal/support exact evidence shape across `ops/evidence/production/public-legal-policy.json` and `ops/evidence/production/public-support-billing-policy.json`, requiring Terms, Privacy, Acceptable Use, AI/content disclaimer, IP complaint flow, support contact, report-problem, support SLA, billing/refund/cancellation policy visibility, paid-launch policy alignment, full release SHA matching, safe projection fields, and no blocked/local/dry-run/secret/raw-payload markers. Strict production legal/support evidence remains open because the current files are Stage 0 check-level pass evidence that preserve aggregate production blockers.
- `OP-14`: README and release notes must continue to reflect validator results only. The `VF-7` production launch contract and `scripts/generate_stage1_production_launch_evidence.py` now exist; the generator can summarize current CI, staging, production split, and child-evidence blockers into `ops/evidence/production/stage1-production-launch.json` and `.ndjson`, but it only emits `pass`/`go` after all upstream release gates, exact CI evidence, strict staging evidence, and production child evidence pass.

## Validation Gaps

- `VF-1`: `scripts/validate_stage1_blueprint.py` validates blueprint structure.
- `VF-2`: Batch generation, user batch progress polling, result placement, safety export state, rendered export assets, batch result storage, batch quota reconciliation, local batch quota runtime replay, provider-cost reconciliation, rate-limit/spend-cap, API error taxonomy, support ticket linking, trace projection, canvas/assets/tenant, canvas interaction, canvas versioning, and edit tools/mask validators exist, are wired into `scripts/repo_validate.sh`, and pass against the current local fixtures and contract anchors.
- `VF-2i`: Canvas/assets/tenant local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `backend/internal/canvas`, `backend/internal/assets`, OpenAPI `CanvasNode`/`Asset` schemas, tenant-isolation migration constraints, `PostgresBatchResultSink` persistence anchors, focused Go tests, and `fixtures/stage1/canvas_assets/local_contract.json`. Real rendered asset bytes, signed URL/retention proof, API-backed UI reads, and staging runtime evidence remain open.
- `VF-2ic`: Edit tools/mask local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `backend/internal/edittools`, source-aligned masks, crop/rotate/flip/remove-background/upscale/erase/expand tools, original-asset retention, derived asset revisions, provider `image.edit` request construction, no raw provider payload persistence/projection, secret-like request/mask rejection, frontend `EditToolsPanel`, `Apply Edit Tool` CSRF guard metadata, local derived canvas node and asset-library projection, focused Go/web tests, and `fixtures/stage1/edit_tools_mask/local_contract.json`. Real provider edit adapter behavior, deployed API persistence, staging Playwright evidence, and production security evidence remain open.
- `VF-2id`: Mention parser/picker local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `web/lib/mentions.ts`, `web/lib/mentions.test.ts`, Workspace Brief UI anchors, `@object`, `@asset`, `@brand`, `@skill`, allowed `@model` parsing, Chinese/space labels, duplicate de-duplication, unresolved mention counts, non-allowed model blocking, and `fixtures/stage1/mentions/local_contract.json`. Staging prompt composer evidence remains open.
- `VF-2if`: Result placement local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `web/lib/result-placement.ts`, `web/lib/result-placement.test.ts`, API refresh projection from succeeded child tasks, generated-layer canvas nodes, Asset Library entries, brief-to-result edges, version snapshots, Workspace result-placement attributes, and `fixtures/stage1/result_placement/local_contract.json`. Real provider asset bytes, rendered thumbnails/posters, signed URL/retention proof, deployed backend result visibility, and staging runtime evidence remain open.
- `VF-2ig`: Safety export state local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `web/lib/safety-export-state.ts`, `web/lib/safety-export-state.test.ts`, `SafetyExportStateEvidence`, blocked export reasons, QA/safety/admin-review counts, disabled blocked share controls, zero blocked download/share CTAs, Workspace package/export UI attributes, CSS blocked reason treatment, and `fixtures/stage1/safety_export_state/local_contract.json`. Strict staging export review evidence, real signed URL download evidence, rendered export assets, object retention proof, and production security evidence remain open.
- `VF-2ih`: Rendered export asset local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `RenderedExportAssetEvidence`, `assets/local-rendered-asset-manifest.json`, deterministic local SVG payloads under `assets/rendered/`, browser ZIP generation, Workspace rendered asset/download attributes, smoke artifact payload lists, zero `assets/README.txt` placeholder payloads, and `fixtures/stage1/rendered_export_asset/local_contract.json`. Real provider asset bytes, backend PNG/SVG/PDF/PSD rendering, staging signed URL evidence remains open, object retention cleanup evidence, and production security evidence remain open.
- `VF-2j`: Asset library and Brand Kit local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `backend/internal/assets/library.go`, `backend/internal/brandkit`, migration `0019_stage1_asset_library_brand_kits.sql`, OpenAPI `AssetLibraryEntry`/`BrandKit` routes and schemas, focused Go tests, backend read/write route handlers, tenant-scoped repository methods, principal-tenant projection/write tests, frontend picker/write-client anchors, user-route smoke evidence, and `fixtures/stage1/asset_library_brandkit/local_contract.json`. Deployed staging runtime evidence and production security evidence remain open.
- `VF-3`: Provider registry contract validator exists, and provider sandbox evidence validator/smoke contracts exist. The provider sandbox smoke now omits and overwrites secret-shaped diagnostic bodies before result persistence, so z.ai/OpenAI-compatible selftest failures cannot leave raw keys, bearer headers, webhook signatures, or raw provider payloads in evidence. Strict provider sandbox validation still fails until real production-like staging evidence is generated under `ops/evidence/staging/stage1-provider-sandbox.json`; localhost/private dev-port evidence requires explicit local-devport validation, writes to the `ops/evidence/staging/local-devport/` debug path by default, and must not close staging gates.
- `VF-3b`: Provider edit adapter local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `backend/internal/provider/adapters/edit`, `image.edit`, remove-background/upscale/erase/expand, aligned mask input, result asset projection, OpenAI-compatible edit capabilities, provider registry user projection, focused Go tests, and `fixtures/stage1/provider_edit_adapter/local_contract.json`. Real provider edit runtime, staging provider edit evidence, API-backed edit persistence, and production security evidence remain open.
- `VF-3c`: Provider video adapter local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `backend/internal/provider/adapters/video`, `video.generate`, `video.status`, duration/aspect/first-last-frame/status-polling/storage-result, video/poster storage projection, focused Go tests, and `fixtures/stage1/provider_video_adapter/local_contract.json`. Real provider video runtime, staging provider video evidence, API-backed video persistence, and production security evidence remain open.
- `VF-4`: Stripe staging lifecycle smoke plus evidence validator now exist and are wired into `scripts/repo_validate.sh` in `--contract-only` mode. The smoke now overwrites secret-shaped Stripe/API diagnostic bodies and clears their evidence refs before result persistence, so failed checkout/webhook/admin probes cannot leave raw Stripe keys, webhook signatures, bearer tokens, or z.ai-shaped keys in evidence. The validator has an explicit local-devport mode for debugging and rejects localhost/private/non-HTTPS evidence in default strict mode. Strict production-like staging remains a launch gate until the same evidence is generated against the deployed staging surface.
- `VF-4a`: Team/seat billing backend/API/OpenAPI/generated-client/web-guard/admin-control contract validator exists and is wired into `scripts/repo_validate.sh`; it now includes Stripe subscription-item quantity/proration sync anchors through `team_billing_links`, `team_seat_billing_syncs`, user Team Seats UI, admin team create/invite/remove controls, admin seat-usage review, admin billing-link management API, and admin quota-page controls. Real Stripe proration execution evidence and staging team-seat evidence remain open.
- `VF-4b`: Admin billing ops backend/API/OpenAPI/generated-client/admin-control contract validator exists and is wired into `scripts/repo_validate.sh`; it covers manual credit, refund note, subscription sync, account lock, CSRF/idempotency/rationale controls, admin quota-page forms, and static admin test anchors. Real Stripe refund execution, quota reset automation, and staging evidence remain open.
- `VF-4c`: User billing invoice/receipt backend/API/OpenAPI/generated-client/web UI contract validator exists and is wired into `scripts/repo_validate.sh`; it covers Stripe invoice/receipt URL mapping, `GET /billing/invoices`, `Refresh Invoices`, local invoice fixtures, invoice/receipt link rendering, and user-route/session-security smoke anchors. Real Stripe invoice/receipt staging evidence remains open.
- `VF-4d`: Local Stripe lifecycle reconciliation validator exists and is wired into `scripts/repo_validate.sh`; it covers webhook event summary reconciliation, subscription status projection, refund-note/manual-credit summaries, quota transaction summaries, due quota reset invocation, replay idempotency, and no raw payload/secret projection. Real Stripe staging lifecycle evidence remains open.
- `VF-5`: Stage 1 safety/QA/eval evidence validator and staging smoke contract exist and are wired into `scripts/repo_validate.sh` in `--contract-only` mode. The validator rejects localhost/private/non-HTTPS evidence in default strict mode and has an explicit local-devport mode for debugging only. Strict mode remains a staging launch gate and requires canonical `ops/evidence/staging/stage1-safety-qa-eval.json` plus `.ndjson` evidence covering batch, edit, import, export, support, and admin-review checks.
- `VF-5b`: Export ops local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers list/regenerate/override/cleanup routes, export override SQL persistence, audit-before-persistence, CSRF/idempotency, OpenAPI/generated admin operations, admin API fallback mapping, export retention/signed URL/provenance fields, and admin server actions. Real staging export override, signed URL, retention cleanup, object storage cleanup, and rendered export evidence remain open.
- `VF-5d`: Export manifest/render local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `backend/internal/export`, required export package entries, QA/safety/trace/license/disclaimer fail-closed gates, placeholder rendered asset blocking, PSD layer-manifest-only policy, safe storage keys, focused Go tests, and `fixtures/stage1/export_manifest_render/local_contract.json`. Real PNG/SVG/PDF rendering, real PSD rendering, signed URL staging evidence, object retention cleanup evidence, and production security evidence remain open.
- `VF-5e`: Eval/skillbook local contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `backend/internal/eval`, `backend/internal/skillbook`, required eval suite coverage, stored result read-without-rerun rules, deterministic runner/source fixture digests, skill review/eval/canary/rollback gates, safe user projection, and `fixtures/stage1/eval_skill_release/local_contract.json`. Staging and production skill release/eval/canary evidence remain open.
- `VF-5f`: QA-8 local security scan contract validator exists and is wired into `scripts/repo_validate.sh`; it covers `scripts/security_scan_smoke.sh`, named z.ai/OpenAI-compatible key shape and Stripe key/webhook shape scans, backend security redaction tests, provider OpenAI-compatible redaction, edit/video raw-payload containment, Stripe checkout/webhook live/test and signature anchors, `web`/`admin` moderate+ dependency audit gating, the closed release image scan set `zenari-stage0-backend`、`zenari-stage0-web`、`zenari-stage0-admin`, backend runtime target coverage, batch result projection, asset/support/export/trace redline checks, rate-limit raw-secret projection guards, Go 1.26.4 backend/Docker toolchain pinning, and `fixtures/stage1/security_scan/local_contract.json`. Strict staging scan evidence and Production security launch checks remain open.
- `VF-5g`: Export object access local contract validator exists and is wired into `scripts/repo_validate.sh`; `scripts/validate_stage1_export_object_access_contract.py` covers `fixtures/stage1/export_object_access/local_contract.json`, runtime `WithDownloadURLSigner(api.SignDownloadURL)` wiring, server-mediated signed URL download path, active-retention metadata guard, audit/analytics recording, duplicate/expired/tampered/unsafe signed URL denial, response header containment, export manifest storage-key-only enforcement, local/S3 retention marker cleanup, and focused Go tests across `backend/internal/server`, `backend/internal/stage0`, `backend/internal/objectstore`, and `backend/internal/export`. This is local contract evidence only; strict staging signed URL and retention cleanup evidence remain open, as do real provider asset bytes and production object storage evidence.
- `VF-6`: `scripts/validate_stage1_staging_runtime.py`, `scripts/generate_stage1_staging_runtime_evidence.py`, and `fixtures/stage1/staging_runtime/local_contract.json` now define the aggregate staging runtime gate in contract-only mode. The validator and generator include secret-rejection selftests for forbidden secret/raw payload fields plus Bearer, Stripe key/signature, Postgres URL, and z.ai-shaped strings, so aggregate runtime readiness cannot regress into accepting secret-bearing child evidence. The generator is conservative: missing or blocked child evidence produces a blocked aggregate with `stage1_staging_runtime_evidence_incomplete`; only exact pass evidence plus strict child validator success can produce `release_gate_decision=go`. The strict validator intentionally remains a launch gate and requires canonical `ops/evidence/staging/stage1-runtime.json` plus `.ndjson` with exact pass evidence for auth/RBAC/tenant/audit, batch, staging quota replay, provider sandbox, Stripe test lifecycle, object storage retention cleanup, safety/QA/eval, observability, backup/restore, OP-7 load, and legal/support; it rejects blocked, local, dry-run, preserved do-not-launch, secret, raw payload, DB URL, and missing child-validator evidence.
- `VF-6a`: Release readiness admin contract validator exists and is wired into `scripts/repo_validate.sh`; it proves the Admin `/release` panel reads validator evidence only from `ops/evidence/staging/stage1-runtime.json` and `ops/evidence/production/stage1-production-launch.json`, exposes contract/strict validator/generator anchors, shows Stage 0 gate and CI blockers, derives an Evidence Closure Queue for exact staging quota replay, OP-7 load, object retention, CI artifacts, and production backup/rollback split evidence, preserves safe projection flags, and has no manual go controls. This UI contract does not close strict staging or production launch gates.
- `VF-6h`: Audit Log Search admin contract validator exists and is wired into `scripts/repo_validate.sh`; it proves the Admin `/audit` panel exposes safe search facets, filtered audit result counts, an audit export manifest, field allowlist, denied raw-field list, immutable row counts, and second-review-open counts through `stage1.audit-search-export-local-contract` without projecting raw provider payloads, hidden prompts, secrets, signed URLs, Stripe payloads, cookies, authorization headers, or webhook signatures. This is a local admin governance contract only; strict staging auth/RBAC/tenant/audit evidence and production governance/security evidence remain open.
- `VF-6i`: Operations Incident Runbook admin contract validator exists and is wired into `scripts/repo_validate.sh`; it proves the Admin `/operations` panel exposes incident fields, alert route fields, rollback evidence refs, preserved Do-Not-Launch conditions, blocked gate checks, and a role-scoped action matrix through `stage1.operations-incident-runbook-local-contract` without manual go controls. This is a local operations governance contract only; strict staging observability/backup/load and exact production backup/rollback/incident evidence remain open.
- `VF-6j`: Support Admin Deletion Governance contract validator exists and is wired into `scripts/repo_validate.sh`; it proves the Admin `/support` panel exposes account/project/export/billing erasure review requests, support ticket links, abuse hold refs, trace/asset/export/billing refs, retained evidence, retention boundaries, second-review state, denied raw projection fields, preserved Do-Not-Launch conditions, and blocked gate checks through `stage1.support-admin-deletion-governance-local-contract` without deletion mutation controls. This is a local support governance contract only; strict staging legal/support, production legal/support, production security, and paid billing evidence remain open.
- `VF-6d`: Staging object retention exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/staging_object_retention/local_contract.json`, canonical `ops/evidence/staging/object-storage-retention-cleanup.json` plus `.ndjson`, signed URL split evidence binding, retention policy, expired export cleanup, orphan cleanup, audit refs, admin auth/identity, CSRF, request-id echo, cleanup audit linkage, canonical pass paths, safe projection fields, and default strict failure unless canonical object retention cleanup evidence is strict-pass. Current canonical object retention files are pass-shaped but target reserved `example.test` fixture URLs, so strict validation rejects them as non-real-staging evidence; this gate remains open and must not be inferred from signed URL, local-devport, dry-run, fixture, or checked-in pass-shaped evidence alone.
- `VF-6e`: Staging observability/backup/load exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/staging_observability_backup_load/local_contract.json`, combined preflight `ops/evidence/staging/20260527T013207Z-staging-observability-backup-load-36222.json`, source observability, backup/restore, load, and post-deploy-smoke files, release SHA matching, private-beta gate fixture closure, all required verified entries, safe projection fields, and no blocked/local/dry-run/raw/secret evidence. This exact validator is now a strict child validator for both aggregate staging observability and backup_restore components.
- `VF-6f`: Staging legal/support exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/staging_legal_support/local_contract.json`, canonical `ops/evidence/staging/legal-pages-external-user.json` and `ops/evidence/staging/support-contact-external-user.json`, external-user Terms, Privacy, Acceptable Use, AI/content disclaimer, IP complaint, support contact, report-problem, billing policy, support-ticket context capture, safe projection fields, and no blocked/local/dry-run/raw/secret evidence. The aggregate staging runtime now requires this exact child validator instead of accepting legal/support split JSON shape alone.
- `VF-6g`: Staging auth/RBAC/tenant/audit exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/staging_auth_rbac_tenant_audit/local_contract.json`, canonical `ops/evidence/staging/20260527T1515Z-auth-rbac-tenant-audit.json`, private-beta gate fixture closure, external-user admin API denial, independent admin session boundary, cross-tenant denial, governed admin RBAC runtime, expired temporary override denial via `rbac-provider-002`, immutable audit linkage, all required admin RBAC evidence IDs, audit refs, safe projection fields, and no blocked/local/dry-run/raw/secret evidence. The aggregate staging runtime now requires this exact child validator instead of accepting auth/RBAC evidence by `status=pass` alone.
- `VF-7`: `scripts/validate_stage1_production_launch.py`, `scripts/generate_stage1_production_launch_evidence.py`, and `fixtures/stage1/production_launch/local_contract.json` now define the aggregate production launch gate in contract-only mode. The validator and generator include secret-rejection selftests for forbidden secret/raw payload fields plus Bearer, Stripe key/signature, and z.ai-shaped strings, so aggregate production launch readiness cannot regress into accepting secret-bearing child evidence. The generator is conservative: missing upstream gate, CI, strict staging, staging release bundle preflight, split backup/rollback, or production child evidence produces a blocked aggregate with `stage1_production_launch_evidence_incomplete`; only all release gates `go`, exact CI evidence, strict staging validation, release bundle `passed/go` with `stage1_staging_runtime_verified=true`, `stage1_quota_replay_verified=true`, `stage1_load_verified=true`, empty `missing_slots`, empty `unverified_slots`, empty `blocking_reasons`, and production child evidence can produce `release_gate_decision=go`. The strict validator intentionally remains a launch gate and requires canonical `ops/evidence/production/stage1-production-launch.json` plus `.ndjson`, all Stage 0 release gate fixtures at `go`, strict Stage 1 staging runtime validation, staging release bundle preflight pass with quota replay and load verified plus no missing/unverified/blocking release bundle rows, exact CI evidence, paid billing lifecycle evidence, backup/restore split evidence, rollback/incident/post-deploy split evidence, security, legal/support, provider/claims, activation/review/audit, abuse, and skill canary production evidence. It rejects blocked, deferred, no-go, local, dry-run, blockers-preserved, non-empty release bundle gaps/blockers, secret, raw payload, and active Do-Not-Launch evidence.
- `VF-7h`: The aggregate production launch contract now declares all Stage 1 production exact validators as strict child validators: production security, provider/claims, governance/release, legal/support, paid billing, and backup/rollback. `scripts/generate_stage1_production_launch_evidence.py` runs those validators before it can emit `release_gate_decision=go`, and `scripts/validate_stage1_production_launch.py` requires their commands in aggregate evidence. This prevents aggregate production launch readiness from being inferred from component JSON shape alone when a matching exact validator still rejects canonical production evidence.
- `VF-7d`: CI exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/ci_exact/local_contract.json`, the three OP-2/OP-3/OP-4 canonical CI evidence paths, Stage 0 CI release-gate anchors, Stage 1 production aggregate CI dependencies, installed workflow references, web/admin/backend validation summaries, Playwright smoke/script coverage for user web, admin web, billing, batch, and canvas, local Playwright coverage/safe-projection reporting, Docker build wrapper coverage for web/admin/backend release images, the Docker exact evidence writer, matching full release SHA, workflow run URL, trigger semantics, Playwright surface coverage, immutable image digests, safe projection fields, and default strict failure unless all three canonical CI files are strict-pass. Worker remains backend runtime coverage rather than a standalone Docker image row, and manager is not a Stage 1 release blocker. `ops/ci/playwright-smoke.spec.ts` now has real browser assertions for the five OP-3 coverage rows: user web, admin web, billing, batch, and canvas; `scripts/playwright_smoke.sh` writes matching evidence refs instead of marking billing/batch/canvas passed from the aggregate script status alone. `scripts/docker_build_smoke.sh` now emits per-image context/digest rows and `scripts/write_stage1_ci_docker_evidence.py` normalizes a passed GitHub Actions Docker run into `ops/evidence/ci/stage0-rev2-docker-image-build.json`. Real GitHub Actions CI artifacts remain open and must not be replaced by local `repo_validate.sh` output.
- `VF-7f`: Production provider/claims exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/production_provider_claims/local_contract.json`, canonical `ops/evidence/production/provider-mode.json` and `ops/evidence/production/public-paid-real-generation-claims.json`, Stage 0 provider-or-comp-only anchors, Stage 1 production aggregate provider/claims dependencies, provider registry/strategy group/secret-ref UI anchors, provider cost and sandbox validator anchors, real-provider vs invite/comp-only launch modes, no dev-provider public routing, no silent fallback, monitoring/cost refs, public paid and real-generation claim gating, release SHA matching across both provider files, safe projection fields, and default strict failure unless both canonical production provider files are strict-pass. The existing production provider files remain Stage 0 check-level evidence with aggregate blockers preserved and must not clear global production launch readiness.
- `VF-7g`: Production governance/release exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/production_governance_release/local_contract.json`, canonical activation review/audit, abuse throttle/hold, and skill release/eval/canary production files, Stage 0 production governance check anchors, Stage 1 production aggregate activation/review/audit, abuse, and skill canary dependencies, admin fixture/test anchors, AD-9 eval/skill release contract anchors, high-risk RBAC, reviewer rationale, second review, audit immutability, activation gates, account hold, rate limit, spend cap or kill switch, owner/risk/eval suite/safety refs/canary metrics/rollback/release notes, release SHA matching across all three files, safe projection fields, and default strict failure unless all three canonical governance/release files are strict-pass. The existing production governance/release files remain `pass_with_blockers_preserved` check-level evidence and must not clear aggregate production launch readiness.
- `VF-7b`: Production paid billing exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/production_billing/local_contract.json`, production aggregate contract anchors, Stage 0 production billing blocker anchors, Stripe checkout/webhook lifecycle anchors, admin billing ops, team seat quantity sync, Stripe live-mode artifact shape, team seat subscription item/proration/idempotency proof, invoice/receipt public-link projection, release SHA matching across both billing files, and default strict failure unless canonical production paid billing files are strict-pass. The existing production billing files are still deferred invite/comp-only blocked evidence and must not clear `production_paid_billing_lifecycle`.
- `VF-7c`: Production security launch exact evidence validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/production_security/local_contract.json`, `ops/evidence/production/20260527T1700Z-security-launch-checks.json`, Stage 0 production security blocker anchors, Stage 1 production aggregate anchors, local QA-8 security scan contract, rate-limit/spend-cap, Stripe live/test, provider registry/key containment, backend redaction, server CSRF middleware, web CSRF contract, admin evidence fixtures, release SHA, safe projection fields, and default strict failure unless the canonical production security file is strict-pass. The existing production security file remains `pass_with_blockers_preserved` check-level evidence and must not clear aggregate production launch readiness.
- `VF-7e`: Production legal/support exact evidence validator exists and is being wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/production_legal_support/local_contract.json`, production aggregate contract anchors, Stage 0 legal/support check-level anchors, web legal policy copy, staging external-user legal/support smoke anchors, admin evidence fixtures, Terms/Privacy/AUP/AI disclaimer/IP complaint/support/report-problem/support SLA/billing refund policy coverage, release SHA matching across both legal/support files, and default strict failure unless both canonical production legal/support files are strict-pass. The existing production legal/support files remain check-level evidence with aggregate blockers preserved and must not clear global production launch readiness.
- `VF-7a`: Production backup/rollback exact split validator exists and is wired into `scripts/repo_validate.sh`; it covers `fixtures/stage1/production_backup_rollback/local_contract.json`, `scripts/production_backup_rollback_split_smoke.sh`, Stage 0 split preflight anchors, production aggregate contract anchors, full 40-character release SHA matching across both exact split files, backup schedule/Postgres restore/object restore/asset lineage/billing ledger/RPO/RTO/audit refs, app rollback/feature flag rollback/backend release image `runtime-worker` rollback (`/app/worker`)/worker drain/migration compatibility/incident alert/post-deploy smoke/upstream gate dependencies/audit refs, safe projection fields, and default strict failure unless canonical production files exist. Real production exact split evidence remains open and must not be replaced by the blocked admin-visible probe or local/dev-port evidence.
- `VF-8`: `repo_validate.sh` now calls the Stage 1 blueprint/scope checks plus Stage 1 batch, provider registry, provider-cost, billing/team-seat, invoice, local Stripe lifecycle reconciliation, Stripe evidence contract, safety/QA/eval evidence contract, skill eval release admin contract, eval/skillbook local release contract, mention parser/picker contract, staging runtime contract/generator, production launch contract/generator validators, and web/admin/manager project checks. It also runs smoke-level secret-body persistence guards for provider sandbox and Stripe staging probes, both aggregate generators against a temporary directory, and requires incomplete evidence to remain blocked/no-go with explicit blockers. Full Stage 1 strict staging/production launch validation remains gated on exact environment evidence.

## Immediate Next Execution Order

1. Run `scripts/generate_stage1_staging_quota_replay_evidence.py` against the deployed staging database/API with `STAGING_DATABASE_URL`, `STAGING_API_URL` or optional `STAGING_QUOTA_REPLAY_API_URL`, `STAGING_QUOTA_REPLAY_TENANT_ID`, and `STAGING_QUOTA_REPLAY_BATCH_ID`, then validate canonical `ops/evidence/staging/stage1-quota-replay.json` plus `.ndjson` with `scripts/validate_stage1_staging_quota_replay_evidence.py`.
2. Replace object-store manifest refs with real provider image bytes, rendered thumbnails/posters, richer asset lineage, signed URL/retention proof, and staging result-sink runtime evidence.
3. Generate live OpenAI-compatible sandbox smoke evidence, attach the local edit and video adapters to real provider runtime and staging evidence, and attach real provider invoice/usage export reconciliation to the new `ProviderCostReconciler` contract once staging provider runs are available.
4. Generate real `BL-9` staging Stripe evidence for test checkout, webhook replay, `livemode=false`, paid/past_due/cancel/refund-credit, invoice/receipt visibility, and quota/account projection using `scripts/stage1_stripe_staging_smoke.sh` plus the strict VF-4 validator.
5. Continue the remaining billing lifecycle with real Stripe refund execution, quota reset automation, full team seat product UI plus Stripe seat proration/sync staging evidence, account UI refresh, and production-ready invoice/receipt staging evidence now that checkout, subscription/quota reads, portal, cancel, invoice-list, local invoice/receipt product UI, admin billing ops API/backend/UI contracts, team/seat backend/API/admin billing-link controls, and the staging Stripe evidence validator are wired.
6. Generate Stage 1 batch Playwright evidence against the deployed backend, including staging polling/SSE cadence and real result asset/canvas visibility. Local Playwright now covers create/progress/retry/cancel UI contracts, but staging evidence remains open.
